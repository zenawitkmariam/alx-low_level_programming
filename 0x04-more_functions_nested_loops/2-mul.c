#include "main.h"
#include <stdio.h>

/**
 * mul - gives product
 * @a: first variable to be multiplied by
 * @b: second variable to be multiplied 
 * Return: 1 if true, 0 if false
 */

int mul(int a, int b)
{
 int sum;
 
 sum = a * b;
    
 return (sum);
}

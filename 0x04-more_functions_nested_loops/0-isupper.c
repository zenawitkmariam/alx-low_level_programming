#include "main.h"

/**
* isupper- tells weather the letter is uppercase or lowercase.
* @l : is the letter to be checked
* Return : 1 if uppercase 0 if not
*/

int isupper(char l)
{
 int i = d;
 if(l>=65 && l<=90)
 {
 return (1);
 }
 else
 {
 return (0);
 }
}
